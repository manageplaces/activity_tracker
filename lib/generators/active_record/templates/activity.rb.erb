class <%= activity_class %> < ActiveRecord::Base

  has_and_belongs_to_many :<%= activity_batch_class.underscore.pluralize %>

  belongs_to :sender, class_name: '<%= user_class %>'
  belongs_to :subject
end
